//-*- mode: c++; indent-tabs-mode: t; tab-width: 4; local-preserve-whitespace: t; -*-

void addGoals(CommandCenter command) {
	command.addGoal("load", WingedGrabber, integer(TitaniumOre)+",4");
	command.addGoal("board", WheeledGrabber, "");
}

extern void object::P2L1() {
	CommandCenter command = new CommandCenter();

	object that = this;
	command.registerSlave(that);
	command.checkForUnregisteredSlaves(that);
	
	command.resetGoals();
	addGoals(command);
	
	//run slave script
	slave();
}
