void addGoals(CommandCenter command) {
	Goal g = new LoadGoal(TitaniumOre, 4);
	command.addGoal(g);
}

void satelliteReport(Base base) {
}

extern void object::P2L1() {
	CommandCenter command = new CommandCenter();
	command.resetGoals();
	addGoals(command);

	Base base = new Base();
	satelliteReport(base);
	base.registerStartingResources(this);

	command.registerSlave(this);
	command.checkForUnregisteredSlaves(this);

	//run slave script
	slave();
}
